import { InputSearch } from './Form.styled';

const Form = () => {
  return (
    <form>
      <label>
        <InputSearch type="search" name="search"></InputSearch>
      </label>
    </form>
  );
};

export default Form;
